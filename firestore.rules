rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Users can only access their own settings, transactions, and inventory items
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
